<UserControl
    x:Class="TimeTableOne.View.Pages.TablePage.Controls.TimeTableGrid"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TimeTableOne.View.Pages.TablePage.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d" d:DataContext="{local:TimeTableGridViewModelInDesign}"
    d:DesignHeight="800"
    d:DesignWidth="1920">
    <Grid Background="BlueViolet">
        <Grid.Resources>
            <DataTemplate x:Key="TimeRegionDataTemplate">
                <local:TimeDisplayUnit></local:TimeDisplayUnit>
            </DataTemplate>
            <DataTemplate x:Key="TimeTableUnitDataTemplate">
                <local:TimeTable></local:TimeTable>
            </DataTemplate>
            <local:TimeTableGridTemplateSelector x:Key="TemplateSelector" TimeRegionTemplate="{StaticResource TimeRegionDataTemplate}" TimeTableUnitTemplate="{StaticResource TimeTableUnitDataTemplate}"></local:TimeTableGridTemplateSelector>
        </Grid.Resources>
        <!--以下ListViewの実装-->
        <ListView ItemsSource="{Binding GridItems}" ItemTemplateSelector="{StaticResource TemplateSelector}">
            <ListView.ItemsPanel>
                <ItemsPanelTemplate>
                    <VariableSizedWrapGrid HorizontalAlignment="Center" Orientation="Horizontal" MaximumRowsOrColumns="{Binding WidthSplit}" ItemWidth="{Binding ElementWidth}" ItemHeight="{Binding ElementHeight}"></VariableSizedWrapGrid>
                </ItemsPanelTemplate>
            </ListView.ItemsPanel>
        </ListView>
    </Grid>
</UserControl>
