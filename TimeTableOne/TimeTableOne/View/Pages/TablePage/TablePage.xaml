<Page
    x:Name="pageRoot"
    x:Class="TimeTableOne.View.Pages.TablePage.TablePage"
    DataContext="{Binding DefaultViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:control="using:TimeTableOne.View.Pages.TablePage.Controls"
    xmlns:common="using:TimeTableOne.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid MinHeight="768" MinWidth="1024" Background="{Binding  colorResource.OneNotePurple}">
        <Grid.RowDefinitions>
            <RowDefinition Height="120"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" Loaded="Grid_Loaded">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton"  HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" 
                        Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"

                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button" />
            <TextBlock x:Name="pageTitle" 
                       Text="TimeTable" 
                       Style="{StaticResource HeaderTextBlockStyle}" 
                       Grid.Column="1" 
                       IsHitTestVisible="false" 
                       TextWrapping="NoWrap" 
                       VerticalAlignment="Center" 
                       HorizontalAlignment="Center"/>
        </Grid>
        <Grid Grid.Row="1" Background="{Binding  colorResource.EditBoxGray}">
            <Grid.RowDefinitions>
                <RowDefinition Height="1*"/>
                <RowDefinition Height="15*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="0"/>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="1">
                <TextBlock Text="Mon" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black" />
            </Grid>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="2">
                <TextBlock Text="Tue" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
            </Grid>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="3">
                <TextBlock Text="Wed" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
            </Grid>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="4">
                <TextBlock Text="Thu" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
            </Grid>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="5">
                <TextBlock Text="Fry" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
            </Grid>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="6">
                <TextBlock Text="Sat" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
            </Grid>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="7">
                <TextBlock Text="Sun" FontSize="30" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="Black"/>
            </Grid>
            <Grid Background="{Binding  colorResource.EditBoxGray}" Grid.Row="0" Grid.Column="8"/>


            <ListView  Grid.Row="1" Grid.Column="1" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding MonTables}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  DataContext="{Binding}">
                            <control:TimeTable/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView  Grid.Row="1" Grid.Column="2" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding TueTables}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  DataContext="{Binding}">
                            <control:TimeTable/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView  Grid.Row="1" Grid.Column="3" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding WedTables}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  DataContext="{Binding}">
                            <control:TimeTable/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView  Grid.Row="1" Grid.Column="4" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding ThuTables}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  DataContext="{Binding}">
                            <control:TimeTable/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView  Grid.Row="1" Grid.Column="5" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding FriTables}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  DataContext="{Binding}">
                            <control:TimeTable/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView  Grid.Row="1" Grid.Column="6" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding SatTables}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  DataContext="{Binding}">
                            <control:TimeTable/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            <ListView  Grid.Row="1" Grid.Column="7" HorizontalAlignment="Stretch"  VerticalAlignment="Stretch" ItemsSource="{Binding SunTables}" >
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  DataContext="{Binding}">
                            <control:TimeTable/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
