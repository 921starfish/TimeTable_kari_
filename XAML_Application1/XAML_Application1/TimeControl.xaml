<UserControl
    x:Class="XAML_Application1.TimeControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XAML_Application1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignWidth="500"
    d:DesignHeight="200" Loaded="UserControl_Loaded">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width= "3*"/>
            <ColumnDefinition Width= "2*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height= "1*"/>
            <RowDefinition Height= "1*"/>
            <RowDefinition Height= "1*"/>
        </Grid.RowDefinitions>
        <TextBlock  Grid.Column ="0" Grid.Row= "0" Grid.RowSpan="3" Text="{Binding TimeText}" TextAlignment="Center" HorizontalAlignment="Stretch" VerticalAlignment="Center" FontSize="100"/>
        <TextBlock  Grid.Column ="1" Grid.Row= "0" Text="{Binding DayText}" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" FontSize="36"/>
        <TextBlock  Grid.Column ="1" Grid.Row= "1" Text="{Binding WeekText}" HorizontalAlignment="Stretch" VerticalAlignment="Top" FontSize="36"/>
        <TextBlock  Grid.Column ="1" Grid.Row= "2" Text="{Binding Number}" HorizontalAlignment="Stretch" VerticalAlignment="Top" FontSize="36"/>
    </Grid>
</UserControl>
